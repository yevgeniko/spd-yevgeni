{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "qmake && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
